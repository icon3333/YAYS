# YouTube Summarizer - Environment Variables Template
# =====================================================
# Copy this file to .env and fill in your credentials
# NEVER commit the .env file to version control!

# ==========================================
# REQUIRED: API Keys and Credentials
# ==========================================

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
# Create new key with appropriate name (e.g., "youtube-summarizer")
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Target Email Address
# Where to send video summaries (your inbox or RSS reader email-to-tag)
# Examples:
#   - Your inbox: your.email@gmail.com
#   - Inoreader: username.1234567890@inoreader.com
TARGET_EMAIL=your.email@example.com

# Gmail SMTP Credentials
# Use an app-specific password (NOT your regular Gmail password)
# Setup: Google Account → Security → 2-Step Verification → App passwords
# Select: Mail → Other (Custom name: "YouTube Summarizer")
SMTP_USER=your.email@gmail.com
SMTP_PASS=abcd efgh ijkl mnop

# ==========================================
# OPTIONAL: Configuration Settings
# ==========================================

# Logging level (DEBUG, INFO, WARNING, ERROR)
# Default: INFO
LOG_LEVEL=INFO

# Check interval in hours (how often to check for new videos)
# Default: 4 hours
# Note: Set to lower values for testing, but 4 hours is recommended
CHECK_INTERVAL_HOURS=4

# Maximum processed videos to track before rotation
# Default: 10000
# When this limit is reached, oldest entries are removed (keeps half)
MAX_PROCESSED_ENTRIES=10000

# Send summaries via email to target address (true/false)
# Default: true
# When disabled, summaries are only stored in database (not emailed)
SEND_EMAIL=true

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================

# 1. OPENAI API KEY:
#    - Go to https://platform.openai.com
#    - Sign up or log in
#    - Navigate to API Keys section
#    - Click "Create new secret key"
#    - Give it a name (e.g., "youtube-summarizer")
#    - Copy the key and paste above
#    - Add payment method in Billing section if needed

# 2. TARGET EMAIL:
#    - Option A: Use your regular email address (e.g., your.email@gmail.com)
#    - Option B: Use RSS reader email-to-tag (e.g., Inoreader: Settings → Preferences → Mail to Tag)
#    - Copy the email address and paste above

# 3. GMAIL APP PASSWORD:
#    - Log into your Google Account
#    - Go to Security settings
#    - Enable 2-Step Verification (if not already enabled)
#    - Search for "App passwords"
#    - Select "Mail" and "Other (Custom name)"
#    - Enter "YouTube Summarizer" as the name
#    - Click "Generate"
#    - Copy the 16-character password (ignore spaces)
#    - Paste above as SMTP_PASS
#    - Your Gmail address goes in SMTP_USER